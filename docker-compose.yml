version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    volumes:
      - ./questions:/app/questions
      - ./public:/app/public
    restart: unless-stopped
